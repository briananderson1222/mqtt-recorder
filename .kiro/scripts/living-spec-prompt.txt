A Rust source file was just saved. Keep this project's specs and docs in sync with the code. Work through these steps:

1. IDENTIFY THE CHANGE. Run `git diff` (and `git diff --stat`) to see what actually changed in the saved file. If the diff is empty or purely cosmetic (formatting, comments, whitespace), stop here and report 'no doc-relevant change'.

2. FIND THE OWNING SPEC. Search `.kiro/specs/*/requirements.md` and `.kiro/specs/*/design.md` for the requirement(s) and design section(s) that cover the changed module or behavior. The specs are `mqtt-recorder` (CLI, record/replay, broker, mirror, CSV format, topics, shutdown) and `csv-validation` (binary payloads, validation, repair).

3a. IF A SPEC COVERS IT — REPAIR DRIFT. Compare the changed behavior against what the spec says. If the code's behavior diverged from the spec (new flag, changed default, new error path, changed semantics), update the affected requirement acceptance criteria and design sections to match the code as-built. Keep edits surgical: touch only the sections that drifted. If the change CONTRADICTS an explicit design decision recorded in the spec, do NOT silently rewrite it — add a `> :warning: DRIFT:` blockquote under the affected section describing the mismatch so a human can arbitrate.

3b. IF NO SPEC COVERS IT — FILL THE GAP. Parts of this codebase grew past the original specs (for example the TUI in `src/tui/`). If the changed module has no owning spec, scaffold one at `.kiro/specs/<module>/requirements.md` and `.kiro/specs/<module>/design.md`, reverse-engineered from the code as-built. Follow the exact structure of the existing specs (Introduction, Glossary, numbered Requirements with user stories and EARS-style acceptance criteria; design with Overview, Architecture, Components). Title it with a `> :memo: DRAFT — generated by the living-spec hook from code as-built; needs human review.` blockquote at the top. Cover the module's real, current behavior — do not invent features.

4. SYNC THE CLI REFERENCE. If `src/cli.rs` changed, update the `### CLI options` tables in README.md to match the clap definitions exactly (flags, descriptions, defaults). Do not touch other README sections.

5. UPDATE THE CHANGELOG. Add or amend an entry under `## [Unreleased]` in CHANGELOG.md following the Keep a Changelog categories (Added/Changed/Fixed/Removed) that describes the code change in one or two user-facing lines. Skip this for pure refactors with no user-visible effect.

6. REPORT. Finish with a short summary: which spec sections were updated, whether drift was flagged for human review, whether a new draft spec was scaffolded, and what changed in README/CHANGELOG. Never commit — leave changes staged in the working tree for the developer to review.